Class: include-filename-attribution (MATTERS) — entry: main.bas
Corpus rows: maritrini (one of three sub-divergences)
A module-level DIM in an included file that is never used: Python attributes W150 to the included file (inc.bas:1); C attributes it to the main file with the include's line number (main.bas:1) — wrong file. Same family as the already-fixed W170/W190 include-filename-attribution fix, but for W150 module vars.
